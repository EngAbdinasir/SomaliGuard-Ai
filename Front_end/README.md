# SomaliGuard AI Frontend

Run locally:

```bash
npm install
npm run dev
```

Configure `.env` from `.env.example`. For online deployment instructions, see
[`../DEPLOYMENT.md`](../DEPLOYMENT.md).

Production build:

```bash
npm run build
```
